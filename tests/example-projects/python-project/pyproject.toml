[tool.poetry]
name = "python-project"
version = "0.1.0"
description = "Example Python project for dep-extractor testing."
authors = ["Example <example@example.com>"]

[tool.poetry.dependencies]
python = ">=3.6"
requests = "^2.25.1"
flask = "^1.1.2"

[tool.poetry.dev-dependencies]
pytest = "^6.2.2"